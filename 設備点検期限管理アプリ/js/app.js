const KEY="inspection-deadline-v1";let items=JSON.parse(localStorage.getItem(KEY)||"null")||[],viewName="dash";const cats=["車両","設備","備品","消耗品","契約・資格","その他"],cycles=["毎月","3か月","6か月","1年","任意設定"];const today=()=>new Date().toISOString().slice(0,10);const save=()=>localStorage.setItem(KEY,JSON.stringify(items));const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));function days(d){return d?Math.ceil((new Date(d)-new Date(today()))/86400000):99999}function state(x){const n=days(x.next);return n<0?["期限超過","over"]:n===0?["本日期限","over"]:n<=7?["7日以内","near"]:n<=14?["14日以内","near"]:n<=30?["30日以内","near"]:["正常",""]}function addMonths(s,m){const d=new Date(s);d.setMonth(d.getMonth()+m);return d.toISOString().slice(0,10)}function nextDate(prev,cycle,custom){if(!prev)return"";if(cycle==="毎月")return addMonths(prev,1);if(cycle==="3か月")return addMonths(prev,3);if(cycle==="6か月")return addMonths(prev,6);if(cycle==="1年")return addMonths(prev,12);return custom?.unit==="日"?new Date(new Date(prev).getTime()+Number(custom.value)*86400000).toISOString().slice(0,10):addMonths(prev,Number(custom?.value||1))}function dash(){const month=today().slice(0,7);app.innerHTML=`<div class="grid"><div class="card">登録件数<b>${items.length}</b></div><div class="card">今月点検予定<b>${items.filter(x=>x.next?.startsWith(month)).length}</b></div><div class="card">期限7日以内<b class="near">${items.filter(x=>days(x.next)>=0&&days(x.next)<=7).length}</b></div><div class="card">期限超過<b class="danger">${items.filter(x=>days(x.next)<0).length}</b></div><div class="card">完了件数<b class="ok">${items.reduce((a,x)=>a+(x.history?.length||0),0)}</b></div></div><div class="panel"><h2>近日期限</h2>${table(items.slice().sort((a,b)=>days(a.next)-days(b.next)).slice(0,10))}</div>`}function table(rows){return rows.length?`<table><thead><tr><th>管理番号</th><th>対象名</th><th>分類</th><th>次回期限</th><th>残り日数</th><th>担当者</th><th>状態</th></tr></thead><tbody>${rows.map(x=>{const s=state(x);return`<tr data-id="${x.id}" class="${s[1]==="over"?'overdue':''}"><td>${x.no}</td><td>${esc(x.name)}</td><td>${x.cat}</td><td>${x.next||''}</td><td>${days(x.next)>9000?'未設定':days(x.next)+'日'}</td><td>${esc(x.person)}</td><td><span class="badge ${s[1]}">${s[0]}</span></td></tr>`}).join('')}</tbody></table>`:'<p class="muted">登録データはありません。</p>'}function list(){app.innerHTML=`<div class="panel"><div class="form"><label>検索<input id="q" placeholder="対象名・内容・担当者"></label><label>分類<select id="fc"><option value="">すべて</option>${cats.map(x=>`<option>${x}</option>`)}</select></label><label>状態<select id="fst"><option value="">すべて</option><option>期限超過</option><option>7日以内</option><option>30日以内</option></select></label></div><div class="actions"><button id="sort">期限の近い順</button></div><div id="rows"></div></div>`;const render=()=>{let r=items.filter(x=>(!q.value||JSON.stringify(x).includes(q.value))&&(!fc.value||x.cat===fc.value)&&(!fst.value||state(x)[0]===fst.value||fst.value==='30日以内'&&days(x.next)<=30||fst.value==='7日以内'&&days(x.next)<=7);rows.innerHTML=table(r)};q.oninput=render;fc.onchange=render;fst.onchange=render;sort.onclick=()=>{items.sort((a,b)=>days(a.next)-days(b.next));render()};render();rows.onclick=e=>{const tr=e.target.closest('tr[data-id]');if(tr)form(tr.dataset.id)}}function form(id=''){const x=items.find(a=>a.id===id)||{cat:cats[0],cycle:'6か月',history:[]};app.innerHTML=`<div class="panel"><h2>${id?'点検対象を編集':'新規登録'}</h2><div class="form"><label>対象名<input id="name" value="${esc(x.name)}"></label><label>分類<select id="cat">${cats.map(v=>`<option ${v===x.cat?'selected':''}>${v}</option>`)}</select></label><label>管理場所<input id="place" value="${esc(x.place)}"></label><label>担当者<input id="person" value="${esc(x.person)}"></label><label>点検・作業名称<input id="task" value="${esc(x.task)}"></label><label>前回実施日<input id="prev" type="date" value="${x.prev||''}"></label><label>点検周期<select id="cycle">${cycles.map(v=>`<option ${v===x.cycle?'selected':''}>${v}</option>`)}</select></label><label>任意設定（数値・単位）<input id="custom" placeholder="例：90日 / 2か月" value="${x.custom?x.custom.value+x.custom.unit:''}"></label><label>次回期限（手動修正可）<input id="next" type="date" value="${x.next||''}"></label><label class="wide">備考<textarea id="note">${esc(x.note)}</textarea></label></div><div class="actions"><button id="calc">期限を自動計算</button><button id="done">点検完了を登録</button><button id="saveBtn">保存</button>${id?'<button id="del" class="danger">削除</button>':''}</div><div id="hist">${id&&x.history?.length?'<h3>点検履歴</h3>'+x.history.map(h=>`<p>${h.date}｜${esc(h.task)}｜担当：${esc(h.person)}<br>${esc(h.issue||'')}</p>`).join(''):''}</div></div>`;calc.onclick=()=>{let m=custom.value.match(/(\d+)\s*(日|か月|月)?/);next.value=nextDate(prev.value,cycle.value,m?{value:m[1],unit:m[2]==='日'?'日':'月'}:null)};saveBtn.onclick=()=>{const n={id:id||crypto.randomUUID(),no:x.no||`CHK-${String(items.length+1).padStart(4,'0')}`,name:name.value,cat:cat.value,place:place.value,person:person.value,task:task.value,prev:prev.value,next:next.value,cycle:cycle.value,custom:null,note:note.value,history:x.history||[]};if(id)items[items.findIndex(a=>a.id===id)]=n;else items.push(n);save();show('list')};done.onclick=()=>{const date=prompt('実施日（YYYY-MM-DD）',today());if(!date)return;const h={date,task:prompt('実施内容',x.task||'定期点検')||'',person:prompt('担当者',x.person||'')||'',issue:prompt('不具合・対応・改善策','')||''};x.history=x.history||[];x.history.push(h);x.prev=date;x.next=nextDate(date,x.cycle,x.custom);if(id)items[items.findIndex(a=>a.id===id)]=x;else{items.push({...x,id:crypto.randomUUID(),no:`CHK-${String(items.length+1).padStart(4,'0')}`})}save();show('list')};if(id)del.onclick=()=>{if(confirm('削除しますか？')){items=items.filter(a=>a.id!==id);save();show('list')}}}function schedule(){const m=today().slice(0,7);app.innerHTML=`<div class="panel"><h2>${m} 点検予定</h2>${table(items.filter(x=>x.next?.startsWith(m)))}</div>`}function history(){const hs=items.flatMap(x=>(x.history||[]).map(h=>({...h,name:x.name,cat:x.cat})));app.innerHTML=`<div class="panel"><h2>点検履歴</h2>${hs.length?hs.sort((a,b)=>b.date.localeCompare(a.date)).map(h=>`<article class="card"><b>${h.date}｜${esc(h.name)}｜${h.cat}</b><p>${esc(h.task)}</p><p>${esc(h.issue||'')}</p><small>担当：${esc(h.person)}</small></article>`).join(''):'<p class="muted">履歴はありません。</p>'}</div>`}function report(){const month=today().slice(0,7);const by=a=>Object.entries(items.reduce((o,x)=>(o[x[a]||'未設定']=(o[x[a]||'未設定']||0)+1,o),{})).map(([k,v])=>`<li>${k}：${v}件</li>`).join('');app.innerHTML=`<div class="two"><div class="panel"><h2>集計</h2><p>登録件数：${items.length}</p><p>今月実施件数：${items.flatMap(x=>x.history||[]).filter(h=>h.date.startsWith(month)).length}</p><p>今月予定件数：${items.filter(x=>x.next?.startsWith(month)).length}</p><p>期限超過件数：${items.filter(x=>days(x.next)<0).length}</p></div><div class="panel"><h2>分類別</h2><ul>${by('cat')}</ul><h2>担当者別</h2><ul>${by('person')}</ul></div></div>`}function backup(){app.innerHTML=`<div class="panel"><h2>バックアップ</h2><div class="actions"><button id="jo">JSON保存</button><button id="co">CSV保存</button><label class="btn">JSON復元<input id="ji" type="file" hidden></label><label class="btn">CSV読込<input id="ci" type="file" accept=".csv" hidden></label></div></div>`;const dl=(n,t,type)=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob(['\uFEFF',t],{type}));a.download=n;a.click()};jo.onclick=()=>dl('点検期限バックアップ.json',JSON.stringify(items,null,2),'application/json');co.onclick=()=>dl('点検期限一覧.csv',['管理番号,対象名,分類,管理場所,担当者,点検内容,前回実施日,次回期限,周期,備考',...items.map(x=>[x.no,x.name,x.cat,x.place,x.person,x.task,x.prev,x.next,x.cycle,x.note].map(v=>`"${String(v||'').replaceAll('"','""')}"`).join(','))].join('\r\n'),'text/csv');ji.onchange=async()=>{if(confirm('現在のデータを置き換えますか？')){items=JSON.parse(await ji.files[0].text());save();show('dash')}};ci.onchange=async()=>{const t=await ci.files[0].text(),lines=t.replace(/^\uFEFF/,'').split(/\r?\n/).filter(Boolean);if(lines.length<2)return alert('CSVデータがありません');const h=lines[0].split(',');const ix=Object.fromEntries(h.map((v,i)=>[v,i]));const imp=lines.slice(1).map((l,n)=>{const c=l.split(',').map(v=>v.replace(/^"|"$/g,'').replaceAll('""','"'));return{id:crypto.randomUUID(),no:c[ix['管理番号']]||`CHK-${n+1}`,name:c[ix['対象名']]||'',cat:c[ix['分類']]||'その他',place:c[ix['管理場所']]||'',person:c[ix['担当者']]||'',task:c[ix['点検内容']]||'',prev:c[ix['前回実施日']]||'',next:c[ix['次回期限']]||'',cycle:c[ix['周期']]||'任意設定',note:c[ix['備考']]||'',history:[]}});const mode=prompt('CSV読込：追加 または 置換','追加');if(mode==='追加'){items.push(...imp)}else if(mode==='置換'){items=imp}else return;save();show('dash')}}function settings(){app.innerHTML=`<div class="panel"><h2>設定</h2><label>事業所名<input id="org" value="${localStorage.getItem('inspection-org')||''}"></label><div class="actions"><button id="saveOrg">保存</button><button id="clear" class="danger">全データ消去</button></div></div>`;saveOrg.onclick=()=>{localStorage.setItem('inspection-org',org.value);alert('保存しました')};clear.onclick=()=>{if(confirm('全データを消去しますか？')){items=[];save();show('dash')}}}function show(v){viewName=v;title.textContent={dash:'ダッシュボード',form:'新規登録',list:'管理一覧',schedule:'点検予定',history:'点検履歴',report:'集計',backup:'バックアップ',settings:'設定'}[v];document.querySelectorAll('nav button').forEach(b=>b.classList.toggle('active',b.dataset.v===v));({dash,form,list,schedule,history,report,backup,settings}[v])()}document.querySelectorAll('nav button').forEach(b=>b.onclick=()=>show(b.dataset.v));sample.onclick=()=>{items=[{id:'s1',no:'CHK-0001',name:'SAMPLE車両01',cat:'車両',place:'SAMPLE拠点',person:'Staff A',task:'定期点検',prev:today(),next:addMonths(today(),1),cycle:'毎月',note:'架空サンプル',history:[]},{id:'s2',no:'CHK-0002',name:'SAMPLE設備01',cat:'設備',place:'SAMPLE拠点',person:'Staff B',task:'安全点検',prev:today(),next:addMonths(today(),6),cycle:'6か月',note:'架空サンプル',history:[]}];save();show('dash')};show('dash');
