# 設備・車両・備品 点検期限管理Webアプリ

`index.html`をChromeまたはEdgeで開くだけで使える、サーバー不要・オフライン対応の期限管理アプリです。データはブラウザのlocalStorageに保存されます。

## 起動

フォルダ内の`index.html`をダブルクリックしてください。最初は「サンプルデータを読み込む」で架空データを表示できます。

初回起動時は、サンプルデータで開始するか、空の状態で開始するかを選択できます。

## 主な機能

- 対象登録、編集、削除、期限の自動計算
- 正常／30日以内／14日以内／7日以内／本日期限／期限超過の判定
- 点検完了と履歴蓄積、次回期限更新
- 管理一覧、月次予定、履歴、集計、検索・絞り込み
- UTF-8 BOM付きCSV出力・読込（追加／置換）
- JSONバックアップ・復元、印刷用データ確認
- 設定画面で事業所名、担当者一覧、分類一覧、期限警告日数を保存
- `クラウド共有.html`でSharePoint／Power Automate、Firebase、Supabase、汎用REST APIへJSONをPOST／PUT保存・読込（接続先URL・トークン設定、最終同期表示）

設定方法とアクセストークンの取扱いは `クラウド共有設定ガイド.md` を確認してください。

販売前テストの詳細は `販売前テスト結果.md` を参照してください。自動操作で確認済みの範囲と手動未確認の範囲を分けて記載しています。

## 注意

ブラウザごとにデータが保存されます。PC変更やブラウザデータ削除に備え、定期的にJSONバックアップを保存してください。サンプルはすべて架空情報です。

月単位の期限計算は同日を原則とし、該当日がない場合は月末になります（例：2026年1月31日＋1か月＝2026年2月28日）。任意周期は「90日」「2か月」の形式で保存され、次回の点検完了計算にも利用されます。JSONは履歴を含む完全バックアップ、CSVは一覧交換用です。

## 最新の販売前修正

データ保護、厳密なJSON/CSV検証、置換前退避と復旧、任意周期・月末補正、点検完了、期限未設定表示、任意月予定、設定反映、入力保持を修正しました。Chrome・Edgeの実ブラウザを使った自動操作、実JSONファイル復元、ブラウザ完全終了後の保持、A4印刷データ生成を確認済みです。実機プリンター、購入者環境、実クラウド、人によるOSダイアログ・Chrome通常画面の総合手動操作は未確認です。
