# 公開前動作確認（最新版販売ZIP）

## 確認方法

PCにインストールされたChromeとEdgeを使い、使い捨てプロファイルで自動操作しました。Chromeの実ブラウザ起動・操作の確認であり、画面を人がクリックする手動テストではありません。販売ZIPを一時フォルダへ解凍し、file://から起動しました。業務データは使用していません。

## 確認済み

- 両アプリのChrome・Edge起動と主要操作。
- 新規登録、任意周期の保存・再表示、点検完了・キャンセル・二重クリック防止。
- JSON保存ボタンでディスクへダウンロードした実ファイルを確認。
- データ消去後、その実ファイルをブラウザのファイル入力へ指定して復元。BOMにも対応。
- 両アプリのブラウザプロセスを完全終了して同じテストプロファイルで再起動し、記録の一致を確認。
- 不正JSON・保存失敗時の既存データ保持、引用符・カンマ・改行を含むCSV復元、絞り込み解除、設定反映。
- 両アプリのA4 PDF生成と印刷用CSSの操作ボタン非表示。
- 代表的なサンプルの印刷用画面画像を目視確認し、文字切れなし。長大な文章・多数件・あらゆる用紙設定を保証するものではありません。

## 未確認

- 実機プリンターの印刷結果。
- 購入者環境での総合確認。
- 実クラウド接続先への通信。
- 人がOSの保存・選択ダイアログを操作する手動テスト。
- 人がChromeの通常画面を操作する総合手動テスト。

## 引継ぎ

以前の「Chrome実機・ブラウザ完全終了再起動未確認」の記録は今回の自動実機確認で更新されました。手動テストと実機プリンター確認を代用したものではありません。Workはこの結果を販売説明文へ反映し、未確認事項を明記してください。公開承認はユーザーが行います。公開操作はしていません。

回帰テスト：tests/edge_sales_review.js（REVIEW_BROWSERでChrome/Edgeを指定可能）。確認後、アプリ本体を変更せず、最新の販売説明文・確認資料・クラウド共有設定ガイドを同梱して販売ZIPを再生成しました。ZIPとアプリフォルダの全ファイル一致を確認済みです。
