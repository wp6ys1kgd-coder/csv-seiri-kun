# CSV整理くん 操作説明書

## このアプリでできること

CSVファイルから、次のデータを自動で整理します。

- すべての項目が空になっている行を削除
- 内容が同じ重複行を1行だけ残して削除
- 選択した列の文字順で並べ替え
- セルの前後にある余分な空白を削除

元のCSVファイルは変更しません。整理結果は新しいCSVとして保存します。

## 起動方法

1. このフォルダーにある「CSV整理くんを起動.bat」をダブルクリックします。
2. 青い文字の「CSV整理くん」画面が表示されたら起動完了です。

Windowsから確認画面が表示された場合は、ファイルの場所がこのフォルダーであることを確認してから実行してください。

## 基本的な使い方

1. 「CSVを選ぶ」を押します。
2. 整理したいCSVファイルを選びます。
3. 必要であれば「並べ替える列」を選びます。
4. 「整理したCSVを保存」を押します。
5. 保存場所とファイル名を確認して「保存」を押します。
6. 完了画面で、処理前後の行数を確認します。

初めて試すときは、`sample` フォルダーの「顧客リスト_サンプル.csv」を使ってください。「氏名」で並べ替えると動作を確認しやすくなります。

## 対応するCSV

- 1行目に列名があるCSV
- UTF-8形式
- Excelなどでよく使われるShift-JIS（CP932）形式

保存結果は、Excelで日本語が文字化けしにくいUTF-8（BOM付き）形式になります。

## 注意点

- 同じ行かどうかは、すべての列の内容を比較して判断します。
- 並べ替えは文字として行います。数字の大小や日付順としての並べ替えには未対応です。
- 元ファイルと同じ名前・同じ場所への上書きはできません。
- 大切なデータは、念のため事前にバックアップしてください。

## 起動できない場合

この試作品の実行にはPython 3とTkinterが必要です。

1. Python 3がインストールされているか確認します。
2. Pythonをインストールするときは、Tkinterを含む標準構成を選びます。
3. インストール後に、もう一度「CSV整理くんを起動.bat」を実行します。

## テスト方法

開発者向けの確認を行う場合は、「テストを実行.bat」をダブルクリックします。4件のテストがすべて `OK` になれば正常です。
