# クラウド共有設定ガイド

両アプリのフォルダにある `クラウド共有.html` から設定します。

## 共通手順

1. 接続先サービスでJSONを受け取るエンドポイントを作成
2. URLと必要なアクセストークンを入力
3. 保存時はPOSTまたはPUTを選択
4. 「クラウドへ保存」で送信
5. 別のPCでは同じURLを設定し「クラウドから読込」を実行

## サービス別の考え方

- SharePoint／Power Automate：HTTP要求を受け取るフローを作成し、SharePoint等へ保存
- Firebase：HTTPS Callable／RESTエンドポイントを用意
- Supabase：テーブルとRLSポリシーを設定し、REST URLを使用
- 汎用REST API：JSONの `data` 配列を受け取れるAPIを用意

## 注意

アクセストークンはブラウザのlocalStorageに保存されます。共有PCでは使用後に設定を消去してください。サービスごとの認証・権限設定は各サービスの管理画面で行います。
